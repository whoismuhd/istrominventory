services:
  - type: web
    name: istrominventory
    env: python
    buildCommand: pip install -r requirements.txt && python data_persistence_guard.py && python deployment_diagnostic.py && echo "🚀 BUILD COMPLETE - NO DATABASE OPERATIONS - DATA IS SAFE"
    startCommand: streamlit run istrominventory.py --server.port $PORT --server.address 0.0.0.0
    envVars:
      - key: PORT
        value: 10000
      - key: DATABASE_URL
        sync: false   # set actual value in the Render dashboard


