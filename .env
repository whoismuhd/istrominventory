# Use the EXTERNAL URL from Render so your laptop can reach it.
# Replace with your actual Render PostgreSQL External URL
# DATABASE_URL=postgresql+psycopg2://USER:PASSWORD@HOST:5432/DBNAME

# For local development without PostgreSQL, leave this commented out
# The app will automatically fall back to SQLite (istrominventory.db)
